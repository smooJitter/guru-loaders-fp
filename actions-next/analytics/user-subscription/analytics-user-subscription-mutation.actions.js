import { withNamespace } from '../../../../src/utils/with-namespace.js';

// --- Analytics User Subscription Mutations ---
// No mutations defined yet, placeholder for future logic

export default withNamespace('analyticsUserSubscription', {
  // Add subscription analytics mutations here as needed
});
