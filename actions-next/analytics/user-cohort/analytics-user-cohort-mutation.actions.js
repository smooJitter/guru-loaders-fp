import { withNamespace } from '../../../../src/utils/with-namespace.js';

// --- Analytics User Cohort Mutations ---
// No mutations defined yet, placeholder for future logic

export default withNamespace('analyticsUserCohort', {
  // Add cohort analytics mutations here as needed
});
